// String, number, undefined, null, boolean, symnbol

const nome = 'Luiz'; //string
const num1 = 10 // number
const num2 = 12.21 // number

let nomeAluno; // undefined = nao aponta pra local nenhuma na mémoria
const sobrenomeAluno = null; // Nulo > nao aponta pra local nenhuma na mémoria
const aprovado = true; // or false | boolean = true,false ( valor lógico )

